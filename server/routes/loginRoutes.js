const express = require("express");
const router = express.Router();

/* ==========> following routes for GET user profiles using AUTH <==========*/
/* ==========> is this redundednt with the home route? <==========*/
