const express = require("express");
const router = express.Router();

/* ==========> following routes for GET for home Routes <==========*/
/* ==========> since there is no way to get there except through loggin in, I dont think it needs a route? <==========*/
